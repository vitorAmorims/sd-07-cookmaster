// const jwt = require('jsonwebtoken');

// const jwtVerify = (req, res, next) => {
//   const { token } = req.headers;
//   try {
//     jwt.decode(token);
//   } catch (error) {
    
//   }
  
// };

// module.exports = {
//   jwtVerify,
// };