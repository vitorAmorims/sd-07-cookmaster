const validationEmail = require('./validationEmail');
const validationAttributes = require('./validationAttributes');
const userValidation = require('./userValidation');
const emailPassInvalid = require('./emailPassInvalid');

module.exports = {
  validationEmail,
  validationAttributes,
  userValidation,
  emailPassInvalid,
};
