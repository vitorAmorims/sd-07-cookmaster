const clientErr = require('./clientErrCodes');
const serverErr = require('./serverErrCodes');
const statusMsgMap = require('./statusMsgMap');
const success = require('./successCodes');

module.exports = {
  clientErr,
  serverErr,
  statusMsgMap,
  success,
};
